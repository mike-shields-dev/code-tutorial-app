{
  "name": "code_tutorial_app_api",
  "version": "1.0.0",
  "scripts": {
    "dev": "docker-compose up -d && cross-env ENV_FILE=.env.development ./scripts/wait-for-it.sh localhost 5432 -s -t 30 -- nodemon src/index.ts",
    "start": "cross-env ENV_FILE=.env.production node dist/index.js",
    "build": "tsc",
    "test": "docker-compose up -d && cross-env ENV_FILE=.env.test ./scripts/wait-for-it.sh localhost 5433 -s -t 30 -- nodemon src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.11",
    "nodemon": "^3.1.4",
    "prisma": "^4.16.2",
    "prisma-erd-generator": "^1.11.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "prisma": {
    "schema": "src/prisma/schema.prisma"
  }
}
